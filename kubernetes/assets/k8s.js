function esc(s){return String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));}
function codeBlock(s){if(!s)return'';return `<pre><button class="copy" type="button">Copy</button><code>${esc(s)}</code></pre>`;}
function initCopy(){document.addEventListener('click',async e=>{if(!e.target.classList.contains('copy'))return;const t=e.target.nextElementSibling?.innerText||'';try{await navigator.clipboard.writeText(t);e.target.textContent='Copied';setTimeout(()=>e.target.textContent='Copy',900)}catch(_){}})}
function initProgress(prefix,total){const boxes=[...document.querySelectorAll('.done')];boxes.forEach(b=>{const k=prefix+b.dataset.id;b.checked=localStorage.getItem(k)==='1';b.addEventListener('change',()=>{localStorage.setItem(k,b.checked?'1':'0');renderProgress()})});function renderProgress(){const n=boxes.filter(x=>x.checked).length;const el=document.querySelector('#progress');if(el)el.innerHTML=`完成 <b>${n}</b> / ${total}`;}renderProgress();}
function initFilter(){const q=document.querySelector('#search'),d=document.querySelector('#domain');if(!q||!d)return;const run=()=>{const s=q.value.trim().toLowerCase(),v=d.value;document.querySelectorAll('[data-card]').forEach(c=>{const ok=(!s||c.innerText.toLowerCase().includes(s))&&(!v||c.dataset.domain===v);c.classList.toggle('hidden',!ok)});};q.addEventListener('input',run);d.addEventListener('change',run);}
function setupStudyPage({storageKey='study-done-',total=0}={}){initProgress(storageKey,total);initFilter();}
initCopy();