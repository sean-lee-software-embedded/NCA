window.KCNA_TEACHING=(function(){
  const R=[
    [/exam|考試地圖/i,{a:'把 KCNA 想成「Kubernetes 駕照筆試」：不要求你現場修引擎，但要知道方向盤、煞車、儀表板各自做什麼。',t:'不要只背名詞比例；每個名詞都要能回答「它解決什麼問題、跟誰合作、壞掉時看哪裡」。',q:(x,n)=>[`你只剩一個週末準備 KCNA，第 ${n} 頁這個主題最有效的讀法是什麼？`,`先用自己的話說出「${x[0]}」的用途，再跑本頁 mini lab，最後做情境判斷；只背答案最容易在換句話問法時失分。`]}],
    [/cloud native|雲原生/i,{a:'像把一間傳統大工廠拆成可獨立擴充、可自動補貨、可隨時觀測的小產線；重點不是「放到雲上」四個字。',t:'Cloud native ≠ public cloud。On‑prem 也可以用 cloud-native 方法。',q:(x)=>['一家公司把 monolith 搬到雲端 VM，但仍手動部署、無自動恢復、無觀測，這就等於 cloud native 嗎？','不等於。Cloud native 更重視容器化、宣告式管理、自動化、可觀測、鬆耦合與可快速交付。']}],
    [/container.*virtual|virtual machine|VM/i,{a:'VM 像「每戶都有完整廚房的公寓」；container 像「共享大樓基礎設施、但房間彼此隔離」。',t:'Container 不是縮小版 VM；它共享 host kernel。',q:()=>['為什麼 container 通常比 VM 啟動更快？','因為 container 不需要為每個實例啟動完整 guest OS，而是共享 host kernel。']}],
    [/OCI|image/i,{a:'Image 像唯讀食譜／模具；Container 是照著食譜真正做出來、正在執行的成品。',t:'tag 可以被重新指向；digest 才是更穩定的內容識別。',q:()=>['同一個 image tag 昨天與今天可能指向不同內容，若要鎖定完全相同版本應優先使用什麼？','使用 image digest（例如 sha256），不要只依賴可變動的 tag。']}],
    [/registry/i,{a:'Registry 像 App Store 的倉庫，repository 像某個 App 的專區，tag 像版本標籤。',t:'Registry 與 repository 不是同義詞。',q:()=>['Pod 出現 ImagePullBackOff，而且是 private registry，第一批要檢查哪些東西？','image 名稱/tag、registry 可達性、憑證/登入權限與 imagePullSecrets。']}],
    [/api.?server/i,{a:'kube-apiserver 像 Kubernetes 的「總機＋櫃台」：kubectl、controller、scheduler 都先跟它講。',t:'核心元件不是各自直接亂改 etcd；API server 是主要入口。',q:()=>['kubectl 完全無法取得任何 cluster 資源時，control plane 最先要確認哪個入口？','先確認 kube-apiserver 是否可用與 /readyz 狀態。']}],
    [/etcd/i,{a:'etcd 像 Kubernetes 的「總帳本」：記的是 cluster state，不是你的網站訂單資料庫。',t:'etcd 不負責排程，也不執行 Pod。',q:()=>['如果 cluster 的物件狀態需要持久保存，核心資料最後存在哪裡？','etcd。Kubernetes API state 由 control plane 維護並持久化到 etcd。']}],
    [/scheduler|Scheduling Pipeline/i,{a:'scheduler 像飯店分房櫃台：先排除不能住的房間，再從可住的房間中挑最合適的。',t:'scheduler 只決定「去哪個 Node」；真正讓 container 跑起來的是 node 上的 kubelet/runtime。',q:()=>['Pod 一直 Pending 並出現 FailedScheduling，你先看 logs 還是 Events？','先看 kubectl describe pod 的 Events，因為 container 甚至可能還沒啟動，logs 未必存在。']}],
    [/controller-manager|Desired State|reconciliation/i,{a:'Controller 像恆溫器：你設定 25°C，它會持續比較「現在」與「目標」，偏掉就修正，而不是只執行一次。',t:'Kubernetes self-healing 的核心是 reconciliation loop。',q:()=>['Deployment 要 3 個 replicas，你手動刪掉 1 個 Pod，為什麼又出現新的？','controller 發現 actual state 只剩 2，會建立新 Pod 把狀態拉回 desired state=3。']}],
    [/kubelet/i,{a:'kubelet 像每台 Worker Node 的現場工頭：接到 PodSpec 後，確認這台機器上的 container 真正照要求運行。',t:'kubelet 是 node agent，不是全 cluster scheduler。',q:()=>['只有某一台 Node 上的 Pods 都無法正常建立，其他 Node 正常，應把 kubelet 放在排查清單的哪裡？','很前面。這是單一 Node 現象，要檢查該 node 的 kubelet、runtime、CNI、disk/memory 等。']}],
    [/CRI|runtime/i,{a:'CRI 像標準插座：kubelet 不必知道每家 runtime 內部怎麼做，只要用共同介面溝通。',t:'Docker Engine 與 Kubernetes runtime 概念不要混在一起；現代 Kubernetes 常見的是 containerd/CRI-O。',q:()=>['kubelet 要求 runtime 建立 Pod sandbox 時，標準介面叫什麼？','CRI（Container Runtime Interface）。']}],
    [/kube-proxy|Service Data Plane/i,{a:'Service 像公司總機號碼；kube-proxy/其他 data plane 元件負責把來電導到真正可用的 Pod。',t:'Service 是 API object；真正轉送資料的實作可能因 iptables/nftables/eBPF 架構而不同。',q:()=>['Service 物件存在但流量仍不通，為什麼不能只說「Service 一定壞了」？','因為還要檢查 EndpointSlice、Pod readiness、targetPort、DNS、NetworkPolicy 與實際 data plane。']}],
    [/Pod(：|$)|最小部署/i,{a:'Pod 像一間套房：裡面可以住一個或多個 container，共享同一個門牌（Pod IP）與部分儲存空間。',t:'Kubernetes 排程的是 Pod，不是個別 container。',q:()=>['兩個 containers 必須共享 localhost 與同一個 volume，最自然的 Kubernetes 邊界是什麼？','把它們放在同一個 Pod；同 Pod containers 共享 network namespace，並可共同掛載 volume。']}],
    [/Multi-container|Sidecar|Init Container/i,{a:'主 container 像主廚；init container 像開店前先備料的人；sidecar 像在旁邊長期協助的收銀/日誌代理。',t:'init container 必須成功完成，主要 containers 才會開始。',q:()=>['主程式一直沒啟動，但 Pod 顯示 Init:Error，應先查誰？','先查 initContainers 的狀態與 logs；主 container 還沒輪到啟動。']}],
    [/YAML|Manifest/i,{a:'YAML 像建築藍圖；apiVersion/kind 說「要蓋什麼」，metadata 是名字，spec 是你要求的設計。',t:'縮排很重要，但更重要的是欄位必須放在正確 schema 位置。',q:()=>['你忘了某欄位到底放在 spec 哪裡，CKA/KCNA 學習時最好用什麼確認？','用 kubectl explain（例如 kubectl explain pod.spec.containers）查看 API schema。']}],
    [/Labels|Selectors|Annotations/i,{a:'Label 像員工胸牌，可被「篩選」；Annotation 像員工檔案備註，通常不是拿來選人的。',t:'Service/Deployment 的 selector 是靠 labels 配對，不是 annotations。',q:(x)=>[`Service 找不到後端 Pod，而你正在讀「${x[0]}」，第一個最實用的比對是什麼？`,'比對 Service selector 與 Pod labels，接著看 EndpointSlice 是否真的有 endpoints。']}],
    [/Namespace/i,{a:'Namespace 像同一棟大樓裡的不同部門樓層：可以有同名資源，但不是所有資源都屬於某個 namespace。',t:'Node、PersistentVolume 等是 cluster-scoped。',q:()=>['你在 namespace dev 建了 Pod web，prod 能不能也有一個叫 web 的 Pod？','可以。名稱唯一性通常是在同一 namespace 範圍內。']}],
    [/ReplicaSet/i,{a:'ReplicaSet 像點名員：要求永遠有 N 個相同 Pod；Deployment 則像主管，還負責版本更新與 rollback。',t:'實務通常讓 Deployment 管 ReplicaSet，不直接手動維護 ReplicaSet。',q:()=>['更新 Deployment image 後看到新舊兩個 ReplicaSet，同時存在是否一定是錯誤？','不一定。Rolling update 過程會以新 ReplicaSet 逐步替換舊 ReplicaSet。']}],
    [/Deployment/i,{a:'Deployment 像服務的版本管理主管：不只維持人數（replicas），還負責 rolling update 與 rollback。',t:'Deployment 最適合典型 stateless 長期服務。',q:()=>['你要讓 nginx 長期跑 3 份，並且未來能 rolling update，最適合哪個 workload controller？','Deployment。']}],
    [/Rolling|Rollback/i,{a:'Rolling update 像餐廳換班：不是把所有舊員工同時請走，而是分批交接，降低中斷。',t:'apply 成功不代表 rollout 成功，要看 rollout status/Pod readiness。',q:()=>['新 image 無法啟動造成 rollout 卡住，最直接的恢復手段之一是什麼？','確認問題後使用 kubectl rollout undo deployment/<name> 回到上一個可用 revision。']}],
    [/StatefulSet/i,{a:'StatefulSet 像「有固定座號與置物櫃」的員工：Pod-0、Pod-1 身分有順序，常搭配穩定 storage。',t:'不是「有資料」就一定 StatefulSet；關鍵是 stable identity/ordering/storage semantics。',q:()=>['資料庫 replicas 需要固定網路識別與各自穩定磁碟，Deployment 和 StatefulSet 哪個更合適？','通常 StatefulSet 更合適。']}],
    [/DaemonSet/i,{a:'DaemonSet 像「每層樓都要有一位保全」：符合條件的每個 Node 通常都要跑一份。',t:'Node-level agent、log collector、CNI/device plugin 常見 DaemonSet。',q:()=>['你要在每個 GPU Node 都部署一個 device plugin agent，哪種 workload 最自然？','DaemonSet。']}],
    [/CronJob|Job/i,{a:'Job 像一次性任務單；CronJob 像鬧鐘，到時間就建立一張新的 Job 任務單。',t:'CronJob 本身會建立 Jobs；長期服務不要用 Job。',q:(x)=>[`「${x[0]}」最容易跟 Deployment 搞混。判斷關鍵是什麼？`,'看工作是否「完成後應結束」。一次性/排程批次用 Job/CronJob；長期服務用 Deployment。']}],
    [/Declarative|Imperative/i,{a:'Imperative 像直接口頭下指令；Declarative 像交付一張「我想要的最終狀態」清單，系統自己持續對齊。',t:'考場可以用 imperative 快速產 YAML，但 Kubernetes 的核心管理模式仍是 declarative desired state。',q:()=>['為什麼 kubectl create ... --dry-run=client -o yaml 對 CKA 很實用？','可以快速產生正確 YAML 骨架，再少量修改，比從空白手寫更快、更不易漏欄位。']}],
    [/kubectl get|describe/i,{a:'get 像病人名單與生命徵象總覽；describe 像病歷摘要，會帶 Conditions、Events、mounts、selectors 等細節。',t:'排錯不要只停在 get。',q:()=>['Pod 顯示 Pending，下一個最有資訊量的 command 通常是什麼？','kubectl describe pod <name>，特別看 Events。']}],
    [/logs|exec/i,{a:'logs 是看 container 對外說了什麼；exec 是走進 container 裡面實際檢查。',t:'CrashLoopBackOff 常需要 kubectl logs --previous。',q:()=>['container 已 crash 並被重啟，你想看上一個 instance 的輸出，要加什麼？','kubectl logs <pod> --previous（多 container 時再加 -c）。']}],
    [/Requests|Limits|QoS/i,{a:'request 像訂位時先保留的座位；limit 像現場最多允許使用的上限。',t:'request 影響 scheduler；memory 超過 limit 可能 OOMKilled。',q:(x)=>[`在「${x[0]}」情境裡，Pod 因 Insufficient cpu 一直 Pending，應先改 CPU limit 還是 request/可用容量？`,'先看 CPU request 與 Node 可 allocatable 容量；scheduler 主要依 request 做 placement。']}],
    [/nodeSelector|Affinity|Topology|Taint|Toleration/i,{a:'nodeSelector/affinity 像「我想去哪裡」；taint 像 Node 掛出「閒人勿入」；toleration 像 Pod 持有通行資格。',t:'Toleration 只表示「不被這個 taint 擋住」，不代表一定會被吸到該 Node。',q:(x)=>[`Pod 因排程條件而 Pending，讀到「${x[0]}」時最該先去哪裡找證據？`,'kubectl describe pod，查看 FailedScheduling Events，再對照 node labels、taints、affinity/topology 條件。']}],
    [/Probe|Liveness|Readiness|Startup/i,{a:'liveness 是「活著嗎？」；readiness 是「現在能接客嗎？」；startup 是「我啟動比較慢，先別急著判我死掉」。',t:'readiness 失敗通常不會直接 restart container，而是讓它暫時不接 Service 流量。',q:()=>['Pod 顯示 Running 但 Service 沒把流量送給它，哪個 probe 最值得先看？','Readiness probe。Pod 可以 Running 但 NotReady。']}],
    [/Service( |$)|ClusterIP|NodePort|LoadBalancer/i,{a:'Service 像穩定的客服總機；Pod 像值班人員，會換班、IP 會變，但客戶不需要記每個人的手機。',t:'Service selector 必須能找到 Ready Pods，port/targetPort 也要正確。',q:(x)=>[`「${x[0]}」存在但連線失敗，你會用哪條固定路徑排？`,'Service spec/selector → EndpointSlice → Pod readiness/port → DNS/NetworkPolicy/data plane。']}],
    [/EndpointSlice/i,{a:'EndpointSlice 像客服總機後面的「目前可接電話人員清單」。',t:'Service 有 ClusterIP 不代表一定有後端 endpoints。',q:()=>['Service DNS 可解析，但 EndpointSlice 沒有任何 endpoint，最常見下一步檢查什麼？','比對 Service selector 與 Pod labels，並確認 Pod readiness。']}],
    [/DNS|CoreDNS/i,{a:'CoreDNS 像公司內線電話簿，把 service 名稱翻成可用位址。',t:'DNS 問題要先證明是解析失敗，不要一開始就重啟 CoreDNS。',q:()=>['client 能 ping IP，卻無法用 web.default.svc.cluster.local 存取，第一個應測什麼？','在 client Pod 內用 nslookup/dig 測 DNS，並檢查 /etc/resolv.conf、kube-dns Service、CoreDNS Pods。']}],
    [/Ingress|Gateway API/i,{a:'Ingress/Gateway API 像大樓入口與分流規則；但只有「門牌規則」還不夠，還需要實際的 controller/data plane 來執行。',t:'建立 Ingress/Gateway object ≠ 流量自然會通。',q:()=>['你 apply 了一個 Ingress，但 cluster 沒有任何 ingress controller，會發生什麼？','API object 可以存在，但通常不會有實際 ingress data plane 幫你轉送流量。']}],
    [/CNI/i,{a:'CNI 像替每個房間接網路線、配 IP、建立路徑的標準工作介面。',t:'CNI 與 Service forwarding/kube-proxy 角色不同。',q:()=>['新 Pod 都拿不到正常 Pod IP，而 API/etcd 正常，哪一層值得優先懷疑？','CNI plugin / node networking。']}],
    [/NetworkPolicy/i,{a:'NetworkPolicy 像辦公室門禁名單：先選中哪些房間受管，再寫哪些來源/目的可以通行。',t:'Policy 是否真的 enforce 取決於所使用的 CNI 是否支援。',q:()=>['你建立了 NetworkPolicy，但使用的 CNI 不支援 NetworkPolicy，能保證流量被擋住嗎？','不能。物件可存在，但 enforcement 需要支援 NetworkPolicy 的 networking implementation。']}],
    [/RBAC/i,{a:'Role/ClusterRole 像權限清單；RoleBinding/ClusterRoleBinding 像把那張清單發給某個人或 ServiceAccount。',t:'RoleBinding 可以在單一 namespace 綁定 ClusterRole。',q:()=>['你不確定 alice 在 dev namespace 能不能 list pods，最快的驗證命令是什麼？','kubectl auth can-i list pods --as=alice -n dev。']}],
    [/ServiceAccount/i,{a:'ServiceAccount 是 workload 的 Kubernetes 身分證，不是一般人的登入帳號。',t:'最小權限原則：不要讓所有 Pods 共用過大的權限。',q:()=>['Pod 要呼叫 Kubernetes API，但只需 read ConfigMaps，應怎麼設計？','建立專用 ServiceAccount，透過 Role/RoleBinding 只授予需要的 ConfigMap read 權限。']}],
    [/Secret/i,{a:'Secret 像上鎖櫃子的「內容分類」，但 base64 只是包裝，不是鎖本身。',t:'base64 encoding ≠ encryption。',q:()=>['看到 Secret YAML 中 data 是 base64，就能說資料已安全加密嗎？','不能。仍需 RBAC、encryption at rest、外部 secret 管理等保護。']}],
    [/PersistentVolumeClaim|PVC/i,{a:'PVC 像使用者開「我要 50Gi、RWO 的儲存」需求單；PV 是實際可供應的倉庫空間。',t:'PVC 是 namespaced；PV 是 cluster-scoped。',q:()=>['PVC 一直 Pending，先看 Pod logs 合理嗎？','通常不合理。先 describe PVC，檢查 StorageClass/provisioner、capacity、access mode、binding Events。']}],
    [/PersistentVolume|PV/i,{a:'PV 像已被 Kubernetes 登記的一塊儲存資源；PVC 是應用來申請它的需求單。',t:'PV 與 PVC 是供應與申請兩個角色。',q:()=>['哪個是 cluster-scoped：PV 還是 PVC？','PV 是 cluster-scoped；PVC 屬於 namespace。']}],
    [/StorageClass|Dynamic Provision/i,{a:'StorageClass 像「儲存方案型錄」；dynamic provisioner 像收到 PVC 訂單後自動開出對應空間的後台。',t:'StorageClass 不是磁碟本身。',q:()=>['PVC 指定 StorageClass 後仍 Pending，哪個元件/設定最值得確認？','對應 provisioner 是否存在且正常、StorageClass 名稱/參數是否正確，以及 PVC Events。']}],
    [/Access Modes|Reclaim|Ephemeral/i,{a:'Access mode 是「這個儲存允許怎麼被掛載」；reclaim policy 是「退租後要留資料還是回收」。',t:'RWO/RWX 不是 Unix file permission。',q:(x)=>[`在「${x[0]}」主題中，若資料必須跨 Pod 重建後保留，emptyDir 合適嗎？`,'通常不合適。emptyDir 跟 Pod lifecycle 綁定；要跨 Pod 保留通常需 PersistentVolume/PVC。']}],
    [/Troubleshooting|Events|Pending|CrashLoop|ImagePull|OOM|FailedMount/i,{a:'排錯像醫生問診：先看症狀與生命徵象，再看檢查報告，不是一進門就開刀。',t:'固定流程比背 100 個 command 更重要：get → describe/events → logs → dependencies → node/components。',q:(x)=>[`遇到「${x[0]}」時，什麼做法最容易浪費時間？`,'沒有證據就一次重啟很多元件或同時改很多設定；這會破壞線索，也無法知道哪個改動真的有效。']}],
    [/CI|CD|GitOps|Helm|Kustomize|Application Delivery|Delivery/i,{a:'Application delivery 像生產線：原始碼進來，經測試、打包、版本化，再用可重複的方法部署與回復。',t:'工具名不要死背，要記它在交付鏈哪一段解決什麼問題。',q:(x)=>[`「${x[0]}」在 cloud-native delivery 裡最重要的價值是什麼？`,`${x[3]||x[2]}；要能把它放回 build/test/package/deploy/observe/rollback 的完整流程中理解。`]}],
    [/Observability|Metrics|Logs|Traces|Prometheus|Grafana|OpenTelemetry/i,{a:'Observability 像汽車儀表板＋行車記錄器＋導航軌跡：metrics 告訴你數值趨勢、logs 告訴你事件文字、traces 告訴你一次請求經過哪裡。',t:'Monitoring 是你預先盯指標；observability 更強調能從外部輸出推理內部狀態。',q:(x)=>[`如果你正在學「${x[0]}」，一個 API latency 突然升高時應如何把三類訊號串起來？`,'先看 metrics 確認何時/哪個服務異常，再用 traces 找慢在哪一段，最後用相關 logs 找具體錯誤或原因。']}],
    [/CNCF|Ecosystem|Community|Microservices|Service Mesh|Envoy/i,{a:'CNCF 像 cloud-native 生態的「基金會＋專案社群」，Kubernetes 只是其中最知名的一個專案。',t:'不要把 CNCF 當成某一家雲端供應商或產品公司。',q:(x)=>[`學「${x[0]}」時，KCNA 真正想確認你會什麼？`,'知道它在 cloud-native 生態中的角色、解決的問題與跟 Kubernetes 的關係，而不是背一堆專案 Logo。']}]
  ];
  function enrich(x,n){
    const t=x[0]||'';
    let hit=R.find(r=>r[0].test(t));
    if(!hit) hit=[null,{a:`把「${t}」先當成一個有明確責任邊界的 Kubernetes/Cloud Native 元件來學：先問它「管什麼、不管什麼、跟誰互動」。`,t:`最容易失分的是把相鄰概念混在一起。請用一句話說出「${t}」與前後一個概念的差別。`,q:(x)=>[`情境題｜${t}：如果你只能用一個觀察或 command 來證明自己真的懂這個主題，你會選什麼？`,x[4]?`先做本頁 mini lab：${String(x[4]).split('\n').slice(0,2).join('；')}，再用輸出解釋為什麼符合「${x[3]}」。`:`先用實際 object/state 驗證「${x[3]}」，不要只背定義。`]}];
    const qa=hit[1].q(x,n);
    return {analogy:hit[1].a,trap:hit[1].t,scenarioQ:qa[0],scenarioA:qa[1],teachback:`不用看答案，用 20 秒向一位剛學 Kubernetes 的同事解釋「${t}」；如果講不出「用途＋邊界＋一個例子」，這頁還不能算完成。`};
  }
  return {enrich};
})();