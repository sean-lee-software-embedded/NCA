window.CKA_DATA=(window.CKA_DATA||[]).concat([{"n":41,"domain":"Services & Networking","weight":"20%","cls":"network","title":"Kubernetes Network Model：Pod IP、Node、Service 的三層","core":"每個 Pod 有自己的 IP；Pod-to-Pod 基本模型不應依賴 NAT；Service 提供穩定虛擬入口。排錯先確認你在測哪一層：Pod IP、Service IP、DNS 名稱或外部入口。","cmds":"kubectl get nodes -o wide\nkubectl get pods -A -o wide\nkubectl get svc -A","drill":"建立兩個 Pod，直接從 A curl B 的 Pod IP，再改用 Service；記錄差異。","verify":"kubectl exec <pod-a> -- curl -sS --max-time 2 http://<pod-b-ip>:<port>\nkubectl get pods -o wide","traps":"一看到網路問題就改 kube-proxy；先分層。","docs":"cluster networking network model pods services"},{"n":42,"domain":"Services & Networking","weight":"20%","cls":"network","title":"Pod-to-Pod Connectivity：從 client 到 server 逐跳驗證","core":"先確認 server container 有 listen、Pod Ready、Pod IP 正確，再從 client curl Pod IP。若直連 Pod IP 都不通，Service selector 還不是第一優先。","cmds":"kubectl exec <server> -- ss -lntp\nkubectl get pod <server> -o wide\nkubectl exec <client> -- curl -v --max-time 3 http://<pod-ip>:<port>","drill":"建立 nginx server 與 curl/busybox client，驗證直接 Pod IP。","verify":"kubectl exec <client> -- wget -qO- http://<pod-ip>","traps":"server process 根本沒 listen 時，修 Service 沒用。","docs":"debug services pod connectivity"},{"n":43,"domain":"Services & Networking","weight":"20%","cls":"network","title":"ClusterIP Service：穩定入口與 Selector","core":"ClusterIP 讓同 cluster clients 以穩定 IP/DNS 存取一組 Pods。核心關係是 Service selector → EndpointSlice addresses → targetPort。","cmds":"kubectl expose deployment web --name=web-svc --port=80 --target-port=80\nkubectl get svc web-svc\nkubectl describe svc web-svc","drill":"建立 Deployment + ClusterIP，從 debug Pod 用 service name curl。","verify":"kubectl get endpointslice -l kubernetes.io/service-name=web-svc\nkubectl run curl --rm -it --restart=Never --image=curlimages/curl -- curl -sS web-svc","traps":"Service 有 ClusterIP 但 endpoints 空，通常 selector/readiness 問題。","docs":"service ClusterIP endpoints selectors"},{"n":44,"domain":"Services & Networking","weight":"20%","cls":"network","title":"NodePort：Service port / nodePort / targetPort","core":"NodePort 在每個 Node 開一個高 port，仍由 Service selector 導向 backend。三個 port 不要混：port=Service 入口；targetPort=Pod；nodePort=Node 對外。","cmds":"kubectl expose deployment web --type=NodePort --port=80 --target-port=80\nkubectl get svc web -o wide\nkubectl get svc web -o jsonpath=\"{.spec.ports[0].nodePort}\"; echo","drill":"建立 NodePort，從可到達 Node 的 client 測 NodeIP:nodePort。kind/Mac 網路映射可能不同，重點先理解物件。","verify":"kubectl describe svc web\nkubectl get endpointslice -l kubernetes.io/service-name=web","traps":"把 nodePort 設成 containerPort；或只測 localhost。","docs":"service NodePort targetPort nodePort"},{"n":45,"domain":"Services & Networking","weight":"20%","cls":"network","title":"LoadBalancer Service：Kubernetes 物件與外部實作分開看","core":"type LoadBalancer 需要 cloud/controller 或像 MetalLB 的實作才會真的有 external IP。CKA lab 若提供 controller 就依題目使用；沒有 controller 時 EXTERNAL-IP Pending 不代表 Service selector 壞。","cmds":"kubectl expose deployment web --type=LoadBalancer --port=80 --target-port=80\nkubectl get svc web -w","drill":"建立 LoadBalancer Service，觀察本地 cluster 與有 LB controller 的環境差異。","verify":"kubectl get svc web -o yaml\nkubectl get endpointslice -l kubernetes.io/service-name=web","traps":"把 EXTERNAL-IP Pending 當 kube-proxy 故障。","docs":"service LoadBalancer external traffic"},{"n":46,"domain":"Services & Networking","weight":"20%","cls":"network","title":"EndpointSlice：Service 真正指到誰","core":"現代 Kubernetes 主要用 EndpointSlice 表示 backend endpoints。Service 不通時，它是最高價值檢查點：有沒有 address？port 對不對？ready condition？","cmds":"kubectl get endpointslice -A\nkubectl get endpointslice -l kubernetes.io/service-name=<svc> -o yaml","drill":"把 Service selector 故意改錯，觀察 EndpointSlice endpoint 消失，再修 selector。","verify":"kubectl get svc <svc> -o jsonpath=\"{.spec.selector}\"; echo\nkubectl get pods --show-labels\nkubectl get endpointslice -l kubernetes.io/service-name=<svc>","traps":"只看 Endpoints 舊資源而忽略 EndpointSlice；新版考試應熟悉 EndpointSlice。","docs":"EndpointSlice service endpoints"},{"n":47,"domain":"Services & Networking","weight":"20%","cls":"network","title":"port / targetPort / named port：最常見 Service 小錯","core":"Service port 是 clients 連的 port，targetPort 是 backend container/POD 真正在聽的 port；targetPort 也可用 named containerPort。題目不通時把 svc、endpoint slice、ss 三者對齊。","cmds":"kubectl get svc <svc> -o yaml\nkubectl get pod <pod> -o yaml | grep -A8 ports\nkubectl exec <pod> -- ss -lntp","drill":"讓 nginx container listen 80，但 Service targetPort 故意 8080；用三層 evidence 找出錯誤。","verify":"kubectl describe svc <svc>\nkubectl get endpointslice -l kubernetes.io/service-name=<svc> -o yaml","traps":"EndpointSlice 有 Pod IP 不代表 port 正確。","docs":"service port targetPort named port"},{"n":48,"domain":"Services & Networking","weight":"20%","cls":"network","title":"Headless Service：沒有 ClusterIP 的 DNS 行為","core":"clusterIP: None 的 headless Service 不做一般虛擬 IP 負載平衡，DNS 可直接回 Pod endpoints；StatefulSet 常用。","cmds":"kubectl create service clusterip headless --clusterip=None --tcp=80:80\nkubectl get svc headless -o yaml","drill":"建立 2 個同 label Pods + headless Service，從 client nslookup/dig 看多個 A records。","verify":"kubectl exec <client> -- nslookup headless\nkubectl get endpointslice -l kubernetes.io/service-name=headless","traps":"以為所有 Service 都一定有 ClusterIP。","docs":"headless service DNS StatefulSet"},{"n":49,"domain":"Services & Networking","weight":"20%","cls":"network","title":"Service DNS：短名、namespace、FQDN","core":"同 namespace 可用 svc 短名；跨 namespace 常用 svc.ns；完整形式通常 svc.ns.svc.cluster.local（cluster domain 可不同）。先用 nslookup，再 curl。","cmds":"kubectl exec <client> -- cat /etc/resolv.conf\nkubectl exec <client> -- nslookup web-svc\nkubectl exec <client> -- nslookup web-svc.default.svc.cluster.local","drill":"建立不同 namespace client，分別測短名與 svc.ns。","verify":"kubectl exec <client> -n <ns> -- nslookup <svc>.<target-ns>","traps":"DNS 能解析不代表 HTTP 一定通；解析與連線分開驗證。","docs":"DNS for services pods namespaces"},{"n":50,"domain":"Services & Networking","weight":"20%","cls":"network","title":"CoreDNS：先看 Service，再看 Pods / ConfigMap / Logs","core":"DNS 失敗排錯順序：client resolv.conf → kube-dns Service/EndpointSlice → CoreDNS Pods → CoreDNS ConfigMap/log。不要直接重裝。","cmds":"kubectl get svc -n kube-system kube-dns\nkubectl get pods -n kube-system -l k8s-app=kube-dns -o wide\nkubectl logs -n kube-system -l k8s-app=kube-dns --tail=100\nkubectl get cm -n kube-system coredns -o yaml","drill":"在 lab 改壞 CoreDNS ConfigMap 前先備份；觀察錯誤後恢復並 restart rollout。","verify":"kubectl rollout status -n kube-system deploy/coredns\nkubectl run dns --rm -it --restart=Never --image=busybox:1.36 -- nslookup kubernetes.default","traps":"把 upstream DNS 問題和 cluster DNS service 問題混在一起。","docs":"debugging DNS resolution CoreDNS"}]);
